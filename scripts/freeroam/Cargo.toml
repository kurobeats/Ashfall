[package]
name = "ashfall-freeroam"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ashfall-script = { path = "../../crates/ashfall-script" }

[profile.release]
opt-level = "s"
lto = true
